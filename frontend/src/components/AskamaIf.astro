---
export interface Props {
  expr: string;
}

const { expr } = Astro.props;
---

<Fragment>
  <Fragment set:html={`{% if ${expr} %}`} />
  <slot />
  <Fragment set:html={"{% endif %}"} />
</Fragment>
